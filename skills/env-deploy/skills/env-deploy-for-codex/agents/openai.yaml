interface:
  display_name: "Environment Deploy for Codex"
  short_description: "Automate local or SSH Linux environment deployment"
  default_prompt: "Use $env-deploy-for-codex to detect this project, plan or apply its local or SSH Linux environment deployment, run applicable tests, and generate deployment artifacts."
