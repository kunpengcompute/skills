# {算子名称} 详细设计文档

| 项目 | 内容 |
| :--- | :--- |
| 拟制 | {作者} |
| 审核 | {审核人} |
| 批准 | {批准人} |
| 日期 | {日期} |
| 版本 | {版本号} |

---

# 1 需求背景

{描述该算子优化的背景、当前现状、为什么要做这个优化}

---

# 2 需求目标

{描述该算子优化的目标、预期收益、覆盖范围}

---

# 3 现状分析

## 3.1 原生Spark流程

{描述原生Spark中该算子的执行流程，包括时序图、关键类、关键方法}

## 3.2 当前问题

{描述当前流程中存在的性能瓶颈或功能缺陷}

---

# 4 总体设计

## 4.1 模块交互图

{提供模块交互图，说明各模块职责}

## 4.2 时序图

{提供时序图，说明整体执行流程}

## 4.3 关键类及接口设计

### 4.3.1 Java侧

{描述新增/修改的Java类、成员变量、方法}

### 4.3.2 Native侧

{描述新增/修改的JNI接口、Native方法}

---

# 5 核心流程设计

## 5.1 主流程

{描述核心执行主流程，包括步骤说明}

## 5.2 异常分支

{描述所有异常/错误分支的处理逻辑}

| 异常场景 | 处理方式 | 触发条件 |
| :--- | :--- | :--- |
| {异常场景1} | {处理方式，如：抛出异常/回退原生} | {触发条件} |
| {异常场景2} | {处理方式} | {触发条件} |

## 5.3 流程图

{提供流程图或时序图，清晰展示主流程和异常分支}

---

# 6 接口设计

{描述对外接口、内部接口、JNI接口等}

---

# 7 配置与参数

## 7.1 开关配置（⚠️ 强制检视项）

| 配置项 | 内容 |
| :--- | :--- |
| **是否有开关** | 有开关 / 无开关 |
| **开关名称** | {具体配置项名称，如：spark.omni.sql.columnar.dataWritingCommand} |
| **默认值** | {默认值，如：true} |
| **开启方式** | {如何开启，如：SET spark.omni.sql.columnar.dataWritingCommand=true} |
| **关闭方式** | {如何关闭，如：SET spark.omni.sql.columnar.dataWritingCommand=false} |
| **生效范围** | {生效范围，如：当前Spark Session} |
| **影响范围** | {影响范围，如：所有INSERT INTO/INSERT OVERWRITE写入操作} |

> 如果无开关，必须明确写"无开关"，以下子项不需要填写。

## 7.2 其他参数

| 参数名 | 默认值 | 说明 |
| :--- | :--- | :--- |
| {参数1} | {默认值} | {说明} |

---

# 8 数据类型支持（⚠️ 强制检视项）

> **注意**：必须逐项说明每种数据类型是"支持"还是"不支持"，不得使用"常见类型""基础类型""部分支持"等模糊表述。

| # | 数据类型 | 是否支持 | 补充说明 |
| :--- | :--- | :--- | :--- |
| 1 | BOOLEAN | 支持 / 不支持 | {补充说明，如无则填"—"} |
| 2 | INT | 支持 / 不支持 | {补充说明} |
| 3 | LONG | 支持 / 不支持 | {补充说明} |
| 4 | DOUBLE | 支持 / 不支持 | {补充说明} |
| 5 | STRING | 支持 / 不支持 | {补充说明} |
| 6 | DATE | 支持 / 不支持 | {补充说明} |
| 7 | DECIMAL64 | 支持 / 不支持 | {补充说明} |
| 8 | DECIMAL128 | 支持 / 不支持 | {补充说明} |
| 9 | SHORT | 支持 / 不支持 | {补充说明} |
| 10 | BYTE | 支持 / 不支持 | {补充说明} |
| 11 | FLOAT | 支持 / 不支持 | {补充说明} |
| 12 | CHAR | 支持 / 不支持 | {补充说明} |
| 13 | VARCHAR | 支持 / 不支持 | {补充说明} |
| 14 | NULL | 支持 / 不支持 | {补充说明} |
| 15 | BINARY | 支持 / 不支持 | {补充说明} |
| 16 | ARRAY | 支持 / 不支持 | {补充说明} |
| 17 | MAP | 支持 / 不支持 | {补充说明} |
| 18 | STRUCT(ROW) | 支持 / 不支持 | {补充说明} |
| 19 | TIMESTAMP | 支持 / 不支持 | {补充说明} |

**不支持类型的处理方式**：{描述遇到不支持类型时的行为，如：回退原生执行 / 抛出异常}

---

# 9 观测算子（⚠️ 强制检视项）

| 检视项 | 内容 |
| :--- | :--- |
| **算子名称** | {Omni算子名称，如：OmniInsertIntoHadoopFsRelationCommand} |
| **触发条件** | {描述算子何时被触发，如：开关开启且数据类型全部为支持类型时触发} |
| **如何判断算子生效** | {描述判断方法，如：EXPLAIN EXTENDED执行计划中出现Omni算子名称} |
| **如何判断回退** | {描述判断方法，如：EXPLAIN EXTENDED执行计划中出现原生算子名称而非Omni算子名称} |
| **如何判断走Omni** | {描述判断方法，如：执行计划中出现Omni算子名称，且执行无报错} |
| **观察方式** | {具体观察方式，如：EXPLAIN EXTENDED查看执行计划 / 日志关键字"xxx" / Spark UI / 执行计划指标} |

---

# 10 测试约束（⚠️ 强制检视项）

| 约束项 | 内容 |
| :--- | :--- |
| **测试方法** | 双路径对比测试法（禁用OMNI路径 vs 启用OMNI路径） |
| **变量控制** | 仅控制"是否启用OMNI优化"单一变量，其余环境、配置、数据、SQL完全一致 |
| **对标基准** | 以同版本Apache Spark原生算子的对外可观测表现为唯一基准 |
| **测试环境** | 专属隔离集群，所有对比测试必须固定环境配置 |
| **数据规范** | 测试数据使用固定随机种子生成，确保可复现；非异常用例INSERT≥20行 |
| **存储格式** | 统一STORED AS ORC |
| **验证方式** | 结果排序后逐行逐列100%匹配；执行状态一致；异常行为一致 |

---

# 11 兼容性（⚠️ 强制检视项）

> **注意**：以下5个维度必须全部覆盖，缺少任一维度将导致检视不通过。

| 兼容性维度 | 覆盖范围 | 说明 |
| :--- | :--- | :--- |
| **OS版本** | 22.03 / 24.03 | {补充说明} |
| **JDK版本** | jdk8 / jdk17 | {补充说明} |
| **机型** | 920B / 920G | {补充说明} |
| **Spark版本** | 331 / 343 / 352 | {补充说明，对应Spark 3.3.1 / 3.4.3 / 3.5.2} |
| **存储格式** | orc / parquet | {补充说明} |

---

# 12 SQL用例示例（⚠️ 强制检视项）

> **注意**：SQL用例应覆盖多种场景（正常、异常、边界、回退等），每条SQL用例必须包含全部9项信息。

## 12.1 正常场景：{用例标题}

| 项目 | 内容 |
| :--- | :--- |
| **测试目的** | {描述测试目的} |
| **前置条件** | {描述前置条件} |
| **输入数据** | {描述测试数据特征} |
| **SQL语句** | {完整SQL语句} |
| **预期结果** | {描述预期结果} |
| **观察点** | {描述观察内容} |
| **是否预期走Omni** | 是 / 否 |
| **是否预期回退** | 是 / 否 |

**SQL语句**：

```sql
DROP TABLE IF EXISTS {表名};
CREATE TABLE IF NOT EXISTS {表名} ({列定义}) STORED AS ORC;
INSERT INTO TABLE {表名} VALUES (...), (...), ...;
SELECT * FROM {表名} ORDER BY id;
```

## 12.2 边界场景：含NULL值/空值

| 项目 | 内容 |
| :--- | :--- |
| **测试目的** | 验证含NULL/空值输入时算子行为正确 |
| **前置条件** | Spark集群已启动、OMNI开关已开启 |
| **输入数据** | 含NULL值、空字符串等边界数据 |
| **SQL语句** | {完整SQL语句} |
| **预期结果** | {描述预期结果} |
| **观察点** | {描述观察内容} |
| **是否预期走Omni** | 是 / 否 |
| **是否预期回退** | 是 / 否 |

## 12.3 回退场景：不支持类型触发回退

| 项目 | 内容 |
| :--- | :--- |
| **测试目的** | 验证不支持数据类型时算子正确回退到原生路径 |
| **前置条件** | Spark集群已启动、OMNI开关已开启 |
| **输入数据** | {不支持类型}数据 |
| **SQL语句** | {完整SQL语句} |
| **预期结果** | 执行未报错，结果正确且与原生一致 |
| **观察点** | EXPLAIN EXTENDED执行计划中不出现Omni算子名称 |
| **是否预期走Omni** | 否 |
| **是否预期回退** | 是 |

## 12.4 多列/组合场景（如适用）

| 项目 | 内容 |
| :--- | :--- |
| **测试目的** | 验证多列同时参与计算时行为正确 |
| **前置条件** | Spark集群已启动、OMNI开关已开启 |
| **输入数据** | 多列不同类型组合数据 |
| **SQL语句** | {完整SQL语句} |
| **预期结果** | {描述预期结果} |
| **观察点** | {描述观察内容} |
| **是否预期走Omni** | 是 / 否 |
| **是否预期回退** | 是 / 否 |

---

# 13 行为细节说明（⚠️ 强制检视项 — 行为可测试性）

> **注意**：以下行为细节是测试设计的关键输入，缺少任何一项都会影响测试用例的生成质量。

| 行为维度 | 内容 |
| :--- | :--- |
| **不支持类型的处理行为** | {遇到不支持类型时的具体行为，如：回退原生执行，不抛异常 / 抛出XXX异常} |
| **NULL值处理** | {输入包含NULL时的处理方式，如：NULL参与计算结果为NULL / 直接跳过NULL行} |
| **空输入处理** | {空表/空分区/空文件时的处理方式} |
| **边界值行为** | {关键类型的边界值处理，如INT最大值、DECIMAL精度溢出、STRING超长等} |
| **异常/回退触发条件** | {列出所有触发回退的具体条件} |
| **多列场景行为** | {多列同时参与时的行为描述} |
| **数据量影响** | {数据量大小对行为的影响} |

---

# 14 设计反向Checklist

| 项目 | 结果 | 补充说明 |
| :--- | :--- | :--- |
| 哪些场景遗漏了？（正常/异常） | {结果} | {补充} |
| 该特性是否需要大内存 | {是/否} | {补充} |
| 对CPU性能和内存影响是否不符合预期？ | {是/否} | {补充} |
| 影响老特性未提前识别？ | {是/否} | {补充} |
| 隔离点有遗漏或处理不妥？ | {是/否} | {补充} |
| 可维护性能力差难以定位和观测效果？ | {是/否} | {补充} |
| 其他补充 | {内容} | |

---

# 15 遗留问题记录

| 编号 | 遗留问题 | 进展 | 状态 |
| :--- | :--- | :--- | :--- |
| 1 | {遗留问题} | {进展} | {状态} |

---

# 16 附录

## 16.1 参数配置清单

| 参数名 | 默认值 | 说明 |
| :--- | :--- | :--- |
| {参数1} | {默认值} | {说明} |

## 16.2 附件

{相关附件列表}
