# Spark OmniOperator <算子名> 测试设计文档

## 一、概述
<简要描述算子功能和测试范围>

## 二、算子签名
- 名称：`<算子类名>`
- 数据流向：`<读取类 | 写入类>`
- 功能语义：`<扫描/过滤投影/聚合/排序/关联/窗口/写入>`
- Omni算子：`<OmniXxxCommand>`
- 开关配置：`<配置项>`（默认启用/禁用）
- 风险等级：`<极高 | 高 | 中>`

## 三、参考实现分析

### 3.1 原生算子
- 算子类名：`<类名>`
- 关键行为：`<描述>`

### 3.2 Omni算子（黑盒视角）
- 触发条件：`<条件>`
- 回退条件：`<条件>`
- 与原生差异：`<无 / 列出差异>`

### 3.3 验证方法
- 结果对比：逐行对比SQL执行结果
- 执行计划验证：EXPLAIN输出中检查Omni算子名称
- 回退验证：不支持类型执行后EXPLAIN无Omni标记

### 3.4 SQL测试模式
- <写入类：DROP → CREATE → INSERT写入 → SELECT验证 → EXPLAIN>
- <读取类：SELECT → EXPLAIN（有固定表）或 DROP → CREATE → INSERT → SELECT → EXPLAIN（无固定表）>

## 四、输入因子

| 编号 | 因子分类 | 因子名称 | 可选值 | 影响权重 |
|:---|:---|:---|:---|:---|
| F001 | `<因子分类>` | `<因子名>` | `<可选值>` | `<极高/高/中>` |

### 4.1 数据类型

| 数据类型 | Spark SQL类型 | 是否支持 | 补充说明 |
|:---|:---|:---|:---|
| BYTE | TINYINT | <是/否> | — |
| SHORT | SMALLINT | <是/否> | — |
| INT | INT | <是/否> | — |
| LONG | BIGINT | <是/否> | — |
| FLOAT | FLOAT | <是/否> | — |
| DOUBLE | DOUBLE | <是/否> | — |
| BOOLEAN | BOOLEAN | <是/否> | — |
| STRING | STRING | <是/否> | <是/否>包含中文 |
| CHAR | CHAR(n) | <是/否> | <是/否>包含中文 |
| VARCHAR | VARCHAR(n) | <是/否> | <是/否>包含中文 |
| DATE | DATE | <是/否> | — |
| DECIMAL64 | DECIMAL(p,s) p≤18 | <是/否> | — |
| DECIMAL128 | DECIMAL(p,s) 18<p≤38 | <是/否> | — |
| TIMESTAMP | TIMESTAMP | <是/否> | — |
| BINARY | BINARY | <是/否> | — |
| ARRAY | ARRAY\<T> | <是/否> | <是/否>包含中文 |
| MAP | MAP\<K,V> | <是/否> | <是/否>包含中文 |
| STRUCT | STRUCT<...> | <是/否> | <是/否>包含中文 |

## 五、测试设计方法

### 5.1 等价类划分法

| 编号 | 功能点 | 等价类描述 | Level |
|:---|:---|:---|:---|
| `<编号>` | `<功能点>` | `<等价类描述>` | `<Level>` |

### 5.2 边界值分析法

| 编号 | 数据类型 | 边界值描述 | Level |
|:---|:---|:---|:---|
| `<编号>` | `<类型>` | `<边界值描述>` | `<Level>` |

### 5.3 场景法

| 编号 | 场景描述 | 操作流程 | Level |
|:---|:---|:---|:---|
| `<编号>` | `<场景>` | `<流程>` | `<Level>` |

### 5.4 判定表法

| 编号 | 条件组合 | 预期动作 | Level |
|:---|:---|:---|:---|
| `<编号>` | `<条件组合>` | `<动作>` | `<Level>` |

### 5.5 正交试验法

| 编号 | 因子组合 | 覆盖场景 | Level |
|:---|:---|:---|:---|
| `<编号>` | `<因子组合>` | `<场景>` | `<Level>` |

### 5.6 错误推测法

| 编号 | 易错场景 | 推测依据 | Level |
|:---|:---|:---|:---|
| `<编号>` | `<场景>` | `<依据>` | `<Level>` |

## 六、测试类型

### 6.1 功能测试

| 编号 | 测试用例 | 测试范围 | Level | 覆盖因子 |
|:---|:---|:---|:---|:---|
| `<编号>` | `<测试用例>` | `<范围>` | `<Level>` | `<覆盖因子>` |

### 6.2 性能测试

| 编号 | 测试用例 | 测试范围 | Level | 覆盖因子 |
|:---|:---|:---|:---|:---|
| `<编号>` | `<测试用例>` | `<范围>` | `<Level>` | `<覆盖因子>` |

### 6.3 可靠性测试

| 编号 | 测试用例 | 测试范围 | Level | 覆盖因子 |
|:---|:---|:---|:---|:---|
| `<编号>` | `<测试用例>` | `<范围>` | `<Level>` | `<覆盖因子>` |

### 6.4 兼容性测试

| 编号 | 测试用例 | 测试范围 | Level | 覆盖因子 |
|:---|:---|:---|:---|:---|
| `<编号>` | `<测试用例>` | `<范围>` | `<Level>` | `<覆盖因子>` |

## 七、用例统计

### 7.1 按测试设计方法统计

| 测试设计方法 | 用例数 | Level0 | Level1 | Level2 |
|:---|:---|:---|:---|:---|
| `<方法>` | `<n>` | `<n>` | `<n>` | `<n>` |

### 7.2 按测试类型统计

| 测试类型 | 用例数 | Level0 | Level1 | Level2 |
|:---|:---|:---|:---|:---|
| `<类型>` | `<n>` | `<n>` | `<n>` | `<n>` |

### 7.3 总计

- 总用例数：`<n>`
- Level0: `<n>` / Level1: `<n>` / Level2: `<n>`

### 7.4 自检结果
